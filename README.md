# Infotecs 2026 Winter Task

## 📌 Описание

Это **REST API на C# (.NET)** для обработки и анализа данных Timescale/CSV, реализованное в рамках **тестового задания на стажировку C# разработчика в компанию «ИнфоТеКС»**. Проект использует **чистую архитектуру (Clean Architecture)** и **CQRS / MediatR**, а также поддерживает асинхронную обработку и фильтрацию результатов.

---

## 🧱 Структура репозитория

```
├── Chronos.sln
├── Chronos.API/                # ASP.NET Core Web API
├── Chronos.Application/        # Application logic (CQRS, DTOs, Commands/Queries)
├── Chronos.Core/               # Domain models and interfaces
├── Chronos.Infrastructure/     # Database, caching, external services
├── example_files/              # Примеры CSV файлов
├── docker-compose.yml          # Сборка сервисов с БД
├── Dockerfile                  # Сборка образа API
├── .env.example                # Пример переменных окружения
└── README.md
```

---

## 💡 Требования

* .NET 10 SDK
* Docker и Docker Compose
* PostgreSQL (через Docker)

---

## 📦 Как запустить

### 🔁 Локально через Docker

1. Склонировать репозиторий:

    ```bash
    git clone https://github.com/topoff0/infotecs-2026-winter-task
    ```

2. Перейти в директорию проекта:
    ```bash
    cd infotecs-2026-winter-task
    ```

3. Скопировать пример конфигурации:

    ```bash
    cp .env.example .env
    ```

4. Запустить сервисы:
    ```bash
    docker compose up --build -d
    ```

5. Остановить приложение:
    ```bash
    docker compose down
    ```

API будет доступен по умолчанию на `http://localhost:8080`

---

## 🧩 Формат ошибок

API возвращает ошибки в едином JSON‑формате:

```json
{
  "code": "ERROR_CODE",
  "description": "ERROR_DESCRIPTION",
  "errorType": (INT)
}
```

